<%= render 'form', debt: @debt %>

    <div class="btn-group w-50 mx-auto my-3" role="group">
      <%= link_to 'Show', edit_debt_path(@debt), class: 'text-dark btn btn-light' %>
      <%= link_to 'Back', debts_path, class: 'text-dark btn btn-light' %>   
    </div>
